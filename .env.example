# Local Postgres (see docker-compose.yml) or Railway Postgres URL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/clearfield"

# Optional: POST /api/credits/purchase-dev
DEV_PURCHASE_SECRET="change-me-local-only"

# Production safety: keep false until Stripe checkout is wired.
ENABLE_MANUAL_PURCHASES="false"
